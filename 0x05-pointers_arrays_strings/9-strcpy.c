#include <stdio.h>
#include "main.h"
/**
 **_strcpy - Copie the string pointed to by src
 *@dest: pointer to char
 *@src: pointer to char
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	if (n >= 0)
	{
		for (i = 0; i < n; i++)
		{
			printf("%d", a[i]);
			if (i == n - 1)
				break;
			printf(", ");
		}
		printf("\n");
	} else
	{
	printf("\n");
	}
}
