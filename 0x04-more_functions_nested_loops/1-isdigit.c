#include "main.h"

/**
 * _isdigit - checks for a digit(0 trough 9)
 * @c: character
 * Return: if is a digit return 1, otherwise 0
 */

int _isdigit(int c)
{
	if (c > 47 && c < 58)
		return (1);

return (0);
}
