#include "main.h"
/**
 *add - Function to return 1 if C is a lower and 0 if C is upper
 *@a: Parameter
 *@b: Parameter
 *Return: value "n"
 */
int add(int a, int b)
{
	int r;

	r = (a + b);
	return (r);
}
